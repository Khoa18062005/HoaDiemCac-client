/**
 * Dữ liệu thực đơn khách hàng - Nhà hàng Hỏa Diệm Các.
 * Đã loại bỏ hoàn toàn mock data để lấy trực tiếp từ MySQL Database qua API /api/v1/menu-items.
 */

export const CUSTOMER_CATEGORIES = [];
export const mockCustomerDishes = [];
export default { CUSTOMER_CATEGORIES, mockCustomerDishes };
