# Thư mục: src/features/menu/api/

## Danh sách các file JS cần có:
- 📄 `menuApi.js`: Các hàm gọi API lấy danh mục món ăn (`getCategories`), danh sách món (`getMenuItems`), và chi tiết món (`getMenuItemDetail`).
