# Thư mục: src/features/cart/api/

## Danh sách các file JS cần có:
- 📄 `cartApi.js`: Các hàm gọi API lấy giỏ hàng (`getCartByTable`), cập nhật số lượng món (`updateCartItem`), và gửi đơn vào bếp (`submitCartToKitchen`).
