# Thư mục: src/features/kitchen/api/

## Danh sách các file JS cần có:
- 📄 `kitchenApi.js`: API lấy danh sách hàng đợi món cần nấu (`getCookingQueue`), cập nhật trạng thái món (`updateItemStatus`), và báo hết món khẩn cấp (`toggleOutOfStock`).
