# Thư mục: src/features/tables/api/

## Danh sách các file JS cần có:
- 📄 `tableApi.js`: API lấy danh sách bàn (`getAllTables`), tạo mã QR động mới (`generateQrToken`), và thu hồi mã QR khi đóng bàn (`revokeQrToken`).
